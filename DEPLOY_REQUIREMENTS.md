# Обязательные требования для деплоя на Netlify

## ✅ Критически важные файлы

### 1. `package.json` (ОБЯЗАТЕЛЬНО)
Должен содержать:
- **Скрипт `build`** - команда для сборки проекта
- **Все зависимости** (`dependencies` и `devDependencies`)
- **Версию Node.js** (опционально, но рекомендуется)

**Пример:**
```json
{
  "scripts": {
    "build": "vite build"  // ← ОБЯЗАТЕЛЬНО!
  },
  "dependencies": {
    "react": "^18.2.0"
  }
}
```

### 2. `netlify.toml` (РЕКОМЕНДУЕТСЯ, но не обязательно)
Упрощает настройку деплоя. Должен содержать:
- **`build.command`** - команда сборки
- **`build.publish`** - папка с собранными файлами

**Пример:**
```toml
[build]
  command = "npm run build"
  publish = "dist"
```

**Без `netlify.toml`** можно указать настройки вручную в панели Netlify.

### 3. Точка входа приложения
- **`index.html`** в корне проекта (для Vite/React)
- Или другой HTML файл, который будет точкой входа

### 4. Исходный код приложения
- Папка `src/` с файлами React
- Или другая структура, в зависимости от фреймворка

## 📋 Структура проекта (минимальная)

```
project/
├── package.json          ← ОБЯЗАТЕЛЬНО
├── index.html            ← ОБЯЗАТЕЛЬНО (для Vite)
├── vite.config.js        ← ОБЯЗАТЕЛЬНО (для Vite)
├── src/                  ← ОБЯЗАТЕЛЬНО
│   ├── main.jsx
│   └── App.jsx
└── netlify.toml          ← РЕКОМЕНДУЕТСЯ
```

## ⚙️ Настройки в Netlify (если нет netlify.toml)

Если файла `netlify.toml` нет, укажите в панели Netlify:

1. **Build command:** `npm run build`
2. **Publish directory:** `dist` (для Vite) или `build` (для Create React App)
3. **Node version:** Обычно определяется автоматически

## 🔍 Что проверяет Netlify

### Автоматическое определение (если нет netlify.toml):
- Ищет `package.json`
- Ищет скрипт `build`
- Пытается определить фреймворк (React, Vue, Angular и т.д.)
- Определяет папку для публикации:
  - `dist` для Vite
  - `build` для Create React App
  - `public` для некоторых других

## ❌ Что НЕ нужно загружать в Git

Убедитесь, что `.gitignore` содержит:
```
node_modules/     ← НЕ загружается (устанавливается на Netlify)
dist/             ← НЕ загружается (собирается на Netlify)
.env.local        ← НЕ загружается (секреты)
```

## ✅ Что ДОЛЖНО быть в Git

- ✅ `package.json` и `package-lock.json`
- ✅ `src/` - весь исходный код
- ✅ `index.html`
- ✅ `vite.config.js` (или другой конфиг)
- ✅ `netlify.toml` (рекомендуется)
- ✅ `.gitignore`

## 🚨 Частые ошибки

### Ошибка 1: "Build command failed"
**Причина:** Нет скрипта `build` в `package.json`
**Решение:** Добавьте `"build": "vite build"` в `scripts`

### Ошибка 2: "Publish directory does not exist"
**Причина:** Неправильная папка в `publish` или сборка не создала файлы
**Решение:** Проверьте, что после `npm run build` создаётся папка `dist`

### Ошибка 3: "Module not found"
**Причина:** Зависимости не указаны в `package.json`
**Решение:** Убедитесь, что все используемые пакеты в `dependencies` или `devDependencies`

### Ошибка 4: "404 Not Found" на роутах
**Причина:** Нет redirect правил для SPA
**Решение:** Добавьте в `netlify.toml`:
```toml
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

## 📝 Чек-лист перед деплоем

- [ ] `package.json` содержит скрипт `build`
- [ ] Все зависимости указаны в `package.json`
- [ ] `netlify.toml` настроен (или настройки указаны в панели)
- [ ] Проект собирается локально (`npm run build`)
- [ ] Папка `dist/` (или другая) создаётся после сборки
- [ ] `.gitignore` исключает `node_modules` и `dist`
- [ ] Все файлы загружены в GitHub
- [ ] Репозиторий подключён к Netlify

## 🎯 Минимальный рабочий пример

**package.json:**
```json
{
  "name": "my-app",
  "scripts": {
    "build": "vite build"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@vitejs/plugin-react": "^4.0.0"
  }
}
```

**netlify.toml:**
```toml
[build]
  command = "npm run build"
  publish = "dist"
```

Этого достаточно для базового деплоя!
